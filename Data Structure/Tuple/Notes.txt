In tuple there has only two functions ---> count and index. Elements can not insert manually. 
    a = (1,7,2,0,5,3,0)
    a.count(0) ==> will return total numbers of 0 in the tuple.
    a.index(1) ==> will return the elements of the 1 index.